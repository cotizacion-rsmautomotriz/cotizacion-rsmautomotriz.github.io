document.addEventListener('DOMContentLoaded', () => {
    // Ejemplo de código JavaScript
    const form = document.querySelector('form');
    if (form) {
        form.addEventListener('submit', (e) => {
            // Puedes agregar validaciones o efectos aquí
        });
    }
});